version: v1.0
name: Minimal Pipeline

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2404

blocks:
  - name: Smoke test
    task:
      jobs:
        - name: Checkout + echo
          commands:
            - checkout
            - echo "semaphore yaml loaded"
